[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "endpoint-openpgp-sequoia"
version = "0.1.0"
requires-python = ">=3.11"
description = "Sequoia OpenPGP backend for Endpoint Protocol"

[tool.maturin]
bindings = "pyo3"
module-name = "endpoint_openpgp_sequoia"
